# Application Settings
PORT=3001
NODE_ENV=development

# Security - Comma-separated list of allowed origins that can embed this app
# Use '*' to allow any origin (not recommended for production)
ALLOWED_ORIGINS=http://localhost:8000,https://nurseio-web.example.com

# JSON Web Token Secret - Used for secure communication
# Generate a secure random string for production
JWT_SECRET=change_this_to_a_secure_random_string

# Token validation settings
# Set to true to enable strict token validation in production
STRICT_TOKEN_VALIDATION=false

# Integration settings
# Set to true to allow the parent app to override document types
ALLOW_PARENT_CONFIG_OVERRIDE=true

# For production, set up storage credentials
# AWS S3 (example)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=
# S3_BUCKET_NAME= 